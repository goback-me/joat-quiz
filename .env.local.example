# Copy to .env.local for local `vercel dev` testing (auto-loaded, gitignored).
# In production these are set as real Environment Variables in the Vercel
# dashboard (Project Settings > Environment Variables) — never commit actual
# secrets to git.

SUPABASE_URL=
SUPABASE_SERVICE_KEY=
WEBHOOK_URL=
